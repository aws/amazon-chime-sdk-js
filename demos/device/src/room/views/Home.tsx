import React from 'react';

const Home = (): JSX.Element => {
  return (
    <div
      style={{
        display: 'flex',
        height: '90vh',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <h1>Meeting Lobby</h1>
    </div>
  );
};

export default Home;
